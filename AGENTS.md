# AGENTS.md

## Cursor Cloud specific instructions

This is the `cursor-default` placeholder repository. It contains no application code, dependencies, or services. It exists as a stub when no real repository is assigned to a Cursor agent session.

- **No build/lint/test commands** are available.
- **No services** need to be started.
- If application code is added in the future, update this file with relevant dev environment instructions.
